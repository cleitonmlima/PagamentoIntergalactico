version: "3.3"
services:
  app:
    build:
      args:
        user: nofaro
        uid: 1000
      context: ./
      dockerfile: Dockerfile
    image: nofaro
    container_name: nofaro-app
    restart: unless-stopped
    working_dir: /var/www/
    volumes:
      - ./:/var/www
    networks:
      - nofaro

  db:
    image: mysql:5.7
    container_name: nofaro-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    volumes:
      - ./docker-compose/mysql:/docker-entrypoint-initdb.d
    networks:
      - nofaro

  nginx:
    image: nginx:alpine
    container_name: nofaro-nginx
    restart: unless-stopped
    ports:
      - 8000:80
    volumes:
      - ./:/var/www
      - ./docker-compose/nginx:/etc/nginx/conf.d/
    networks:
      - nofaro

networks:
  nofaro:
    driver: bridge